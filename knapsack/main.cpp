#include <cmath>
#include <cstdio>
#include <vector>
#include <iostream>
#include <algorithm>
using namespace std;


int main() {
    /* Enter your code here. Read input from STDIN. Print output to STDOUT */
    int T,N;
    cin >> T;
    for(int i=0;i <T;i++)
    {
        int N,K;
        cin >> N >>K;
        int in[N];
        int out[N][K];
        for(int j=0;j<N;j++)
            cin >> in[j];
        for(int j=0;j<N;j++)
            out[j][0]=0;
        for(int j=0;j<N;j++)
        {
            for(int c=0;c<K+1;c++)
            {

            }
        }
    }
    return 0;
}
